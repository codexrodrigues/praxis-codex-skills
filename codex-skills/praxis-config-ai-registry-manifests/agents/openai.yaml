interface:
  display_name: "Praxis Config Registry"
  short_description: "Govern executable AI registry contracts"
  default_prompt: "Use $skill-name to audit the canonical Angular-to-praxis-config AI registry pipeline, persisted component definitions and templates, bootstrap/RAG projections, executable manifests, validators, compilers, derived artifacts, and fail-closed validation."

policy:
  allow_implicit_invocation: true
