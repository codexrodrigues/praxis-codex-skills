interface:
  display_name: "Praxis Fields Option Sources"
  short_description: "Guide optionSource selects and entity lookups"
  default_prompt: "Use $praxis-fields-option-sources to implement or audit @praxisui/dynamic-fields package optionSource, async select, searchable select, or entity lookup flow."
