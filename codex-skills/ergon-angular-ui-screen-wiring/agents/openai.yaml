interface:
  display_name: "Ergon Angular UI Screen Wiring"
  short_description: "Migra telas Ergon para Angular usando o runtime Praxis nativo."
  default_prompt: "Use $ergon-angular-ui-screen-wiring para migrar uma tela XML legada Ergon para Angular com Praxis UI nativo, validando APIs, DTOs, schemas, options, actions/surfaces/capabilities e QA visual."

policy:
  allow_implicit_invocation: true
