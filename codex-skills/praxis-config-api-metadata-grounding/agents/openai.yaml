interface:
  display_name: "Praxis Config Grounding"
  short_description: "Ground AI with API metadata"
  default_prompt: "Use $skill-name to implement or audit scoped Praxis API metadata ingestion, RAG retrieval, AI context/schema resolution, Project Knowledge revalidation, and grounded API Catalog answers."

policy:
  allow_implicit_invocation: true
