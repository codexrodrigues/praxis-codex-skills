interface:
  display_name: "Praxis Form Editor Document"
  short_description: "Round-trip form editor documents"
  default_prompt: "Use $praxis-form-editor-document-roundtrip to inspect DynamicFormAuthoringDocument, Settings Panel apply/save/reset, bindings, context snapshots, diagnostics, and editor round-trip."

policy:
  allow_implicit_invocation: true
