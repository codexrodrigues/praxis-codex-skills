interface:
  display_name: "Praxis Fields Runtime Loader"
  short_description: "Guide dynamic field loader and registry changes"
  default_prompt: "Use $praxis-fields-runtime-loader to review an @praxisui/dynamic-fields package loader, ComponentRegistryService, selector mapping, or runtime coverage change."
