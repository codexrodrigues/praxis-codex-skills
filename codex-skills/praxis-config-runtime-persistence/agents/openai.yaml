interface:
  display_name: "Praxis Config Persistence"
  short_description: "Persist runtime config safely"
  default_prompt: "Use $skill-name to implement or audit Praxis config scope, environment resolution, ETag persistence, Angular storage, and safe enterprise runtime context projections."

policy:
  allow_implicit_invocation: true
