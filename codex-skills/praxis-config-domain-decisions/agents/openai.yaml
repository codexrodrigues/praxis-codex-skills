interface:
  display_name: "Praxis Config Decisions"
  short_description: "Govern semantic decisions"
  default_prompt: "Use $skill-name to inspect praxis-config-starter domain rules, domain knowledge, simulations, publications, materializations, decision diagnostics, explainability, timelines, and evidence lifecycle."

policy:
  allow_implicit_invocation: true
