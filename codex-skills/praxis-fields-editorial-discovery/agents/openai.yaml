interface:
  display_name: "Praxis Fields Editorial Discovery"
  short_description: "Keep dynamic fields discoverable in tooling"
  default_prompt: "Use $praxis-fields-editorial-discovery to update @praxisui/dynamic-fields package editorial descriptors, field catalogs, registry metadata, and tooling discovery."
